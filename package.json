{
  "name": "post-op-care-bot",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:s3": "npm run build && aws s3 sync dist/ s3://%S3_BUCKET%/ --delete",
    "deploy:s3:invalidate": "aws cloudfront create-invalidation --distribution-id %CF_DIST_ID% --paths \"/*\""
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.2.0"
  }
}
